  data:
    redis:
      host: localhost
      port: 6379
      database: 1

logging:
  level:
    com.shop: debug
    org.springframework: info

server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 20161220
