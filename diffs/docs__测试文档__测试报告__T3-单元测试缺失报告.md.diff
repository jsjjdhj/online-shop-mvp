--- current/docs\测试文档\测试报告\T3-单元测试缺失报告.md
+++ unmerged/docs\测试文档\测试报告\T3-单元测试缺失报告.md
@@ -1,27 +1,341 @@
-# T3 单元测试缺失与残留说明

+# T3 单元测试缺失报告

 

 **项目**：在线购物平台 MVP

+**课程**：软件质量与测试课 2025-2026-2 学期

 **任务编号**：T3

-**记录日期**：2026-06-17

-

-## 一、原残留项处理结果

-

-| 残留项 | 处理结果 |

-|---|---|

-| `GET /api/admin/products` 未做管理员权限校验 | **已修复** |

-| JaCoCo 精确覆盖率未生成 | **已修复** |

-| 前端 T3 单元测试仍是样例级 | **暂不处理，仅留档** |

-

-## 二、当前仍保留的缺失项

-

-当前 T3 只剩 1 项未闭环：

-

-| 项目 | 说明 |

-|---|---|

-| 前端 T3 单元测试 | `shop-frontend/tests/unit/example.spec.js` 仍为样例级测试，未形成对管理员前端功能的真实测试覆盖 |

-

-## 三、结论

-

-T3 的后端测试缺失项已全部收口。

-T3 当前唯一保留项为：**前端单元测试未补齐，但已按要求留档。**

-

+**任务名称**：管理员端全栈开发与单测

+**报告日期**：2026-06-16

+

+---

+

+## 一、评估概述

+

+### 1.1 当前测试现状

+

+| 指标 | 当前值 |

+|------|--------|

+| 测试类数量 | 2 个 (OrderServiceTest, UserServiceTest) |

+| 测试用例数量 | 11 个 |

+| 测试通过率 | 100% |

+| Service 接口方法总数 | 20 个 |

+| 已覆盖的方法数 | 12 个 |

+| **接口覆盖率** | **60%** |

+

+### 1.2 核心结论

+

+当前测试通过了全部主流程验证（happy path），但存在以下关键缺陷：

+

+1. **ProductService 完全没有测试** — 这是 T3 任务的核心功能之一（商品管理）

+2. **OrderService 大量异常分支未覆盖** — 状态校验、权限校验等业务逻辑无保障

+3. **CartService 作为核心依赖模块缺少独立测试**

+4. **Controller 层完全未测试**

+

+---

+

+## 二、按 Service 接口逐项分析

+

+### 2.1 OrderService（12 个方法）

+

+| 方法签名 | 是否有独立测试 | 覆盖方式 | 异常路径覆盖 | 评级 |

+|----------|---------------|----------|--------------|------|

+| `submitOrder(Long, OrderSubmitRequest)` | ✅ 有 | 直接调用 | ❌ 仅空车场景 | ⚠️ 部分覆盖 |

+| `listUserOrders(Long, int, int)` | ❌ 无 | — | — | 🔴 缺失 |

+| `getOrderDetail(Long, Long)` | ❌ 无 | — | — | 🔴 缺失 |

+| `payOrder(Long, Long)` | ⚠️ 嵌入 testFullFlow | 正常路径 1 次 | ❌ 未覆盖 | 🔴 缺失异常 |

+| `cancelOrder(Long, Long)` | ✅ 有 | 直接调用 | ❌ 未覆盖 | 🔴 缺失异常 |

+| `completeOrder(Long, Long)` | ⚠️ 嵌入 testFullFlow | 正常路径 | ❌ 未覆盖 | 🔴 缺失异常 |

+| `confirmOrder(Long)` | ⚠️ 嵌入 testFullFlow | 正常路径 | ❌ 未覆盖 | 🔴 缺失异常 |

+| `shipOrder(Long)` | ⚠️ 嵌入 testFullFlow | 正常路径 | ❌ 未覆盖 | 🔴 缺失异常 |

+| `listAllOrders(int, int, Integer)` | ✅ 有 | 基本查询 | ❌ 未按状态筛选 | ⚠️ 部分覆盖 |

+| `getAdminOrderDetail(Long)` | ❌ 无 | — | — | 🔴 缺失 |

+| `getOrderItems(Long)` | ✅ 有 | 直接调用 | N/A | ✅ 覆盖完整 |

+

+**覆盖率统计**：11/12 方法有调用，但仅 4 个有完整的正常+异常覆盖 → **有效覆盖率约 33%**

+

+#### OrderService 缺失详情

+

+**submitOrder 缺失的边界场景**：

+- 商品已下架（status=1）时提交订单 → 应抛出 BusinessException "商品[xx]已下架"

+- 商品库存不足（quantity > stock）时提交 → 应抛出 BusinessException "商品[xx]库存不足"

+

+**状态流转操作的异常路径（均未覆盖）**：

+

+| 操作 | 当前只测了 | 未测的异常输入 | 预期异常信息 |

+|------|-----------|----------------|-------------|

+| `payOrder()` | status=0/1 → 成功 | status=3(已发货) | "订单状态不正确" |

+| `cancelOrder()` | status=0 → 成功 | status=2(已付款) | "已付款订单无法取消" |

+| `completeOrder()` | status=3 → 成功 | status=2(已付款) | "订单未发货" |

+| `confirmOrder()` | status=0 → 成功 | status=1(已确认) | "订单状态不正确" |

+| `shipOrder()` | status=2 → 成功 | status=1(已确认) | "订单未付款" |

+

+**完全缺失的方法级测试**：

+

+| 方法 | 业务意义 | 为什么需要测试 |

+|------|---------|---------------|

+| `listUserOrders()` | 用户查看自己的订单列表 | 需验证 userId 过滤是否生效（不能看到他人订单） |

+| `getOrderDetail()` | 用户查看订单详情 | 需验证权限校验（非本人订单应拒绝） |

+| `getAdminOrderDetail()` | 管理员查看任意订单详情 | 需验证管理员可访问任意订单 |

+

+---

+

+### 2.2 UserService（3 个方法）

+

+| 方法签名 | 是否有独立测试 | 覆盖方式 | 评级 |

+|----------|---------------|----------|------|

+| `register(RegisterRequest)` | ✅ 有 | 3个用例（成功/重复/密码不一致） | ✅ 覆盖完整 |

+| `login(LoginRequest)` | ✅ 有 | 2个用例（错误密码/5次锁定） | ✅ 覆盖完整 |

+| `getCurrentUser(Long)` | ❌ 无 | — | 🔴 缺失 |

+

+**缺失详情**：

+

+| 用例 | 验证点 | 预期结果 |

+|------|--------|----------|

+| getCurrentUser_存在用户 | 传入有效 userId | 返回正确的 User 对象 |

+| getCurrentUser_不存在用户 | 传入无效 userId | 返回 null 或抛异常 |

+

+---

+

+### 2.3 ProductService（5 个方法）— **T3 核心任务，完全无测试**

+

+> ⚠️ **这是最严重的缺口**。T3 任务说明中明确包含「商品新增、编辑、下架」三个子任务，对应 ProductService 的 create/update/delete 方法，但目前没有任何测试覆盖。

+

+| 方法签名 | T3 任务关联 | 是否有测试 | 优先级 |

+|----------|------------|-----------|--------|

+| `createProduct(ProductRequest)` | ✅ 商品新增 | ❌ 无 | P0-必须 |

+| `updateProduct(Long, ProductRequest)` | ✅ 商品编辑 | ❌ 无 | P0-必须 |

+| `deleteProduct(Long)` | ✅ 商品下架 | ❌ 无 | P0-必须 |

+| `getProductById(Long)` | 辅助查询 | ❌ 无 | P1-高 |

+| `listProducts(int, int, String)` | 含关键词搜索逻辑 | ❌ 无 | P1-高 |

+

+**建议补充的 ProductServiceTest 用例**：

+

+| 编号 | 用例名称 | 验证点 | 预期结果 |

+|------|----------|--------|----------|

+| PT-1 | createProduct_Success | 合法参数（名称/价格/库存） | 商品入库，status=0，字段正确 |

+| PT-2 | updateProduct_Success | 修改名称、价格、库存 | 字段更新持久化 |

+| PT-3 | deleteProduct_Success | 调用下架 | status=1（逻辑删除，非物理删除） |

+| PT-4 | updateProduct_NotFound | 编辑不存在的商品 ID | BusinessException "商品不存在" |

+| PT-5 | deleteProduct_NotFound | 删除不存在的商品 ID | BusinessException "商品不存在" |

+| PT-6 | getProductById_NotFound | 查询不存在的 ID | BusinessException "商品不存在" |

+| PT-7 | listProducts_KeywordSearch | 按名称/描述关键词搜索 | 结果集正确过滤 |

+| PT-8 | listProducts_Pagination | 分页参数（page=2, size=5） | 返回第2页，每页5条 |

+

+---

+

+### 2.4 CartService（4 个方法）— 核心依赖模块，间接调用但无独立测试

+

+> CartService 是 submitOrder 的前置依赖（订单需要从购物车结算），其addItem() 在 OrderServiceTest 中作为辅助方法被调用，但其余 3 个方法和 addItem 的异常路径均未覆盖。

+

+| 方法签名 | 是否有独立测试 | 覆盖方式 | 评级 |

+|----------|---------------|----------|------|

+| `addItem(Long, CartRequest)` | ⚠️ 间接 | 仅在 submitOrder 中正常调用 | ⚠️ 缺少异常 |

+| `updateQuantity(Long, Long, CartUpdateRequest)` | ❌ 无 | — | 🔴 缺失 |

+| `removeItem(Long, Long)` | ❌ 无 | — | 🔴 缺失 |

+| `listCartItems(Long)` | ❌ 无 | — | 🔴 缺失 |

+

+**addItem 缺失的异常路径**：

+

+| 场景 | 触发条件 | 预期异常 |

+|------|---------|---------|

+| 商品不存在或已下架 | productId 无效 / status=1 / deleted=1 | "商品不存在或已下架" |

+| 库存不足 | quantity > stock | "商品库存不足" |

+| 数量超限 | 同一商品累加后 > 99 | "数量不能超过99" |

+| 更新后库存不足 | 已有数量 + 新增数量 > stock | "商品库存不足" |

+

+**建议补充的 CartServiceTest 用例**：

+

+| 编号 | 用例名称 | 验证点 | 预期结果 |

+|------|----------|--------|----------|

+| CT-1 | addItem_Success | 新增购物车项 | 记录创建，quantity 正确 |

+| CT-2 | addItem_Duplicate | 再次添加同一商品 | quantity 累加 |

+| CT-3 | addItem_ProductOffline | 商品 status=1 | BusinessException |

+| CT-4 | addItem_InsufficientStock | quantity > stock | BusinessException |

+| CT-5 | addItem_ExceedMaxQty | 累加 > 99 | BusinessException |

+| CT-6 | updateQuantity_Success | 修改为合法数量 | 数量更新 |

+| CT-7 | updateQuantity_Zero | 设为 0 | 记录删除 |

+| CT-8 | updateQuantity_InsufficientStock | quantity > stock | BusinessException |

+| CT-9 | removeItem_Success | 删除已有记录 | 记录消失 |

+| CT-10 | removeItem_NotOwner | 删除他人记录 | BusinessException |

+| CT-11 | listCartItems_Success | 查询购物车 | 返回该用户的购物车列表 |

+

+---

+

+### 2.5 Controller 层

+

+| Controller | 端点数 | 是否有测试 | 说明 |

+|------------|-------|-----------|------|

+| AdminController | 8 个 | ❌ 无 | 需要 JWT Token 生成工具配合 |

+| OrderController | — | ❌ 无 | — |

+| ProductController | — | ❌ 无 | — |

+| CartController | — | ❌ 无 | — |

+| UserController | — | ❌ 无 | — |

+

+> 注：Controller 层因涉及 JWT 认证拦截器，测试复杂度较高，文档中标注为「暂跳过」。如需补充，需先准备 JWT 工具类和 MockMvc 配置。

+

+---

+

+## 三、优先级排序与实施计划

+

+### 3.1 优先级定义

+

+| 优先级 | 含义 | 准则 |

+|--------|------|------|

+| P0 - 必须 | T3 任务直接要求的功能 | 不补则任务不完整 |

+| P1 - 高 | 高风险业务逻辑漏洞 | 异常路径可能导致生产事故 |

+| P2 - 中 | 功能完整性 | 接口未被任何测试触及 |

+| P3 - 低 | 辅助模块 / 复杂度高 | 可后续迭代补充 |

+

+### 3.2 补充计划总览

+

+```mermaid

+flowchart TB

+subgraph P0["P0: 必须补充（T3核心功能缺失）"]

+direction TB

+A1["新建 ProductServiceTest.java\n≥ 8 个用例"]

+A1 --> A1a["PT-1~3: CRUD 主流程"]

+A1 --> A1b["PT-4~6: 异常路径"]

+A1 --> A1c["PT-7~8: 搜索与分页"]

+end

+

+subgraph P1["P1: 高优先级（业务逻辑漏洞风险）"]

+direction TB

+B1["OrderServiceTest 补充异常用例\n≥ 7 个"]

+B1 --> B1a["OT-7~11: 状态校验异常"]

+B1 --> B1b["OT-12~13: 提交订单边界"]

+end

+

+subgraph P2["P2: 中优先级（功能完整性）"]

+direction TB

+C1["UserServiceTest 补充\n≥ 1 个用例"]

+C2["OrderServiceTest 补充\n≥ 5 个用例"]

+end

+

+subgraph P3["P3: 低优先级（辅助模块）"]

+direction TB

+D1["新建 CartServiceTest.java\n≥ 11 个用例"]

+D2["AdminControllerTest\n需 JWT 工具配合"]

+end

+

+P0 --> P1 --> P2 --> P3

+```

+

+### 3.3 详细用例清单

+

+#### P0: ProductServiceTest（新建文件）

+

+| 编号 | 用例名称 | 类型 | 验证点 |

+|------|----------|------|--------|

+| PT-1 | createProduct_Success | 正常 | 创建成功，status=0，各字段正确 |

+| PT-2 | updateProduct_Success | 正常 | 修改后字段值更新 |

+| PT-3 | deleteProduct_Success | 正常 | 下架后 status=1 |

+| PT-4 | updateProduct_NotFound | 异常 | ID 不存在 → BusinessException |

+| PT-5 | deleteProduct_NotFound | 异常 | ID 不存在 → BusinessException |

+| PT-6 | getProductById_NotFound | 异常 | ID 不存在 → BusinessException |

+| PT-7 | getProductById_Deleted | 异常 | deleted=1 → BusinessException |

+| PT-8 | listProducts_KeywordSearch | 正常 | 关键词匹配 name/description |

+| PT-9 | listProducts_Pagination | 正常 | 分页参数生效 |

+

+#### P1: OrderService 异常路径（追加到现有 OrderServiceTest）

+

+| 编号 | 用例名称 | 类型 | 验证点 |

+|------|----------|------|--------|

+| OT-7 | payOrder_WrongStatus | 异常 | status=3 时付款 → 异常 |

+| OT-8 | cancelOrder_AlreadyPaid | 异常 | status=2 时取消 → "已付款" |

+| OT-9 | completeOrder_NotShipped | 异常 | status=2 时完成 → "未发货" |

+| OT-10 | confirmOrder_NotPending | 异常 | status=1 时确认 → 异常 |

+| OT-11 | shipOrder_NotPaid | 异常 | status=1 时发货 → "未付款" |

+| OT-12 | submitOrder_ProductOffline | 异常 | 下架商品 → "已下架" |

+| OT-13 | submitOrder_InsufficientStock | 异常 | 库存不足 → "库存不足" |

+

+#### P2: 功能完整性（分别追加到现有测试类）

+

+**UserServiceTest 追加**：

+

+| 编号 | 用例名称 | 类型 | 验证点 |

+|------|----------|------|--------|

+| UT-7 | getCurrentUser_Success | 正常 | 返回正确 User 对象 |

+

+**OrderServiceTest 追加**：

+

+| 编号 | 用例名称 | 类型 | 验证点 |

+|------|----------|------|--------|

+| OT-14 | listUserOrders_OwnOnly | 正常 | 只返回该用户订单 |

+| OT-15 | getOrderDetail_WrongUser | 异常 | 他人订单 → "订单不存在" |

+| OT-16 | getAdminOrderDetail_Success | 正常 | 返回任意订单 |

+| OT-17 | listAllOrders_ByStatus | 正常 | 按 status 筛选结果正确 |

+

+#### P3: CartServiceTest（新建文件）

+

+| 编号 | 用例名称 | 类型 | 验证点 |

+|------|----------|------|--------|

+| CT-1 | addItem_Success | 正常 | 新建购物车项 |

+| CT-2 | addItem_DuplicateMerge | 正常 | 同商品累加 |

+| CT-3 | addItem_ProductOffline | 异常 | 下架商品 |

+| CT-4 | addItem_InsufficientStock | 异常 | 库存不足 |

+| CT-5 | addItem_ExceedMax99 | 异常 | 超过99限制 |

+| CT-6 | updateQuantity_Success | 正常 | 修改数量 |

+| CT-7 | updateQuantity_DeleteWhenZero | 正常 | 设0则删除 |

+| CT-8 | updateQuantity_OverStock | 异常 | 超库存 |

+| CT-9 | removeItem_Success | 正常 | 删除记录 |

+| CT-10 | removeItem_NotOwner | 异常 | 他人记录 |

+| CT-11 | listCartItems_Success | 正常 | 列表查询 |

+

+---

+

+## 四、预期效果

+

+### 4.1 补充前后对比

+

+| 指标 | 当前 | 补充后（目标） |

+|------|------|---------------|

+| 测试类数量 | 2 | 4 (+ProductServiceTest, +CartServiceTest) |

+| 测试用例数量 | 11 | **36** (+25) |

+| Service 接口覆盖率 | 60% (12/20) | **95%** (19/20) |

+| 异常路径覆盖率 | ~20% | **>75%** |

+| T3 任务功能覆盖 | 商品管理 0% | **100%** |

+

+### 4.2 风险评估

+

+| 风险项 | 当前风险等级 | 补充后 |

+|--------|-------------|--------|

+| 商品管理逻辑回归 | 🔴 高（无任何保护） | 🟢 低 |

+| 订单状态非法流转 | 🟠 中（无状态守卫测试） | 🟢 低 |

+| 购物车并发/边界问题 | 🟠 中（无独立测试） | 🟢 低 |

+| 权限越权访问 | 🟡 中低（部分未测） | 🟢 低 |

+

+---

+

+## 五、附录

+

+### A. 文件结构参考

+

+```

+shop-backend/src/test/java/com/shop/service/

+├── OrderServiceTest.java      ← 已有，需追加 ~12 个用例

+├── UserServiceTest.java       ← 已有，需追加 ~1 个用例

+├── ProductServiceTest.java    ← ❌ 待新建（P0）

+└── CartServiceTest.java       ← ❌ 待新建（P3）

+```

+

+### B. 测试技术栈（与现有一致）

+

+| 项目 | 配置 |

+|------|------|

+| 框架 | SpringBootTest + JUnit 5 |

+| 数据库 | MySQL（@Transactional 回滚） |

+| 断言 | JUnit Assertions + assertThrows |

+| 执行顺序 | @TestMethodOrder(MethodOrderer.OrderAnnotation.class) |

+

+### C. 参考文档

+

+- [T3 单元测试计划与报告](./T3-单元测试计划与报告.md)

+- [OrderServiceImpl 源码](../../../main/java/com/shop/service/impl/OrderServiceImpl.java)

+- [ProductServiceImpl 源码](../../../main/java/com/shop/service/impl/ProductServiceImpl.java)

+- [CartServiceImpl 源码](../../../main/java/com/shop/service/impl/CartServiceImpl.java)

+- [UserServiceImpl 源码](../../../main/java/com/shop/service/impl/UserServiceImpl.java)

+- [AdminController 源码](../../../main/java/com/shop/controller/AdminController.java)

+

+---

+

+*报告生成时间：2026-06-16*

+*基于代码静态分析 + 测试逐行对比*

