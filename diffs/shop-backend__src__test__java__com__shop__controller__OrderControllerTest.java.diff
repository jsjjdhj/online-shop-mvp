--- current/shop-backend\src\test\java\com\shop\controller\OrderControllerTest.java
+++ unmerged/shop-backend\src\test\java\com\shop\controller\OrderControllerTest.java
@@ -1,138 +1,147 @@
 package com.shop.controller;

 

 import com.fasterxml.jackson.databind.ObjectMapper;

-import com.shop.util.JwtUtil;

-import org.junit.jupiter.api.BeforeEach;

-import org.junit.jupiter.api.Test;

+import com.shop.ShopApplication;

+import com.shop.util.TestHelper;

+import org.junit.jupiter.api.*;

 import org.springframework.beans.factory.annotation.Autowired;

 import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;

 import org.springframework.boot.test.context.SpringBootTest;

 import org.springframework.http.MediaType;

 import org.springframework.test.web.servlet.MockMvc;

+import org.springframework.test.web.servlet.request.RequestPostProcessor;

+import org.springframework.transaction.annotation.Transactional;

 

-import static org.hamcrest.Matchers.*;

-import static org.junit.jupiter.api.Assertions.*;

 import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;

 import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

 

-@SpringBootTest(classes = com.shop.ShopApplication.class)

+@SpringBootTest(classes = ShopApplication.class)

 @AutoConfigureMockMvc

+@Transactional

+@TestMethodOrder(MethodOrderer.OrderAnnotation.class)

 class OrderControllerTest {

 

 @Autowired private MockMvc mockMvc;

-@Autowired private JwtUtil jwtUtil;

-private final ObjectMapper objectMapper = new ObjectMapper();

+@Autowired private ObjectMapper objectMapper;

 

-private String userToken;

+private static final long USER_ID = 200L;

 

-@BeforeEach

-void setUp() {

-userToken = jwtUtil.generateToken(1L, "testuser", "USER");

+private static RequestPostProcessor withUser(Long userId) {

+String token = TestHelper.createUserToken(userId, "user_" + userId);

+return request -> {

+request.addHeader("Authorization", "Bearer " + token);

+return request;

+};

 }

 

-private <B> B withUserAuth(B builder) {

-// 使用反射或直接在 request 中设置属性

-return builder; // 简化：通过 header + requestAttr

+private static RequestPostProcessor withAdmin() {

+String token = TestHelper.createAdminToken(1L, "admin");

+return request -> {

+request.addHeader("Authorization", "Bearer " + token);

+return request;

+};

 }

 

-// OCT-01: submit_成功 — 返回 Orders 对象，status=0（依赖购物车有数据）

-@Test

-void submit_Success() throws Exception {

-String json = objectMapper.writeValueAsString(java.util.Map.of(

-"recipientName", "ZhangSan",

-"recipientPhone", "13800138000",

-"recipientAddress", "Beijing"

-));

-mockMvc.perform(post("/api/orders")

-.header("Authorization", "Bearer " + userToken)

-.requestAttr("userId", 1L)

-.requestAttr("username", "testuser")

-.requestAttr("role", "USER")

-.contentType(MediaType.APPLICATION_JSON)

-.content(json))

-.andExpect(result -> {

-int status = result.getResponse().getStatus();

-String body = result.getResponse().getContentAsString();

-// 200 = 成功，200+code=业务错误 = 购物车为空

-assertTrue(body.contains("\"code\""),

-"submit order 应返回结构化 JSON，实际 status=" + status);

-});

+private long getFirstProductId() throws Exception {

+String resp = mockMvc.perform(get("/api/admin/products")

+.with(withAdmin()))

+.andReturn().getResponse().getContentAsString();

+try {

+var json = objectMapper.readTree(resp);

+var records = json.get("data").get("records");

+if (records != null && records.size() > 0) {

+return records.get(0).get("id").asLong();

+}

+} catch (Exception ignored) {}

+return -1;

 }

 

-// OCT-02: submit_未登录 — userId=null → 异常

-@Test

-void submit_NotLoggedIn() throws Exception {

-String json = objectMapper.writeValueAsString(java.util.Map.of(

-"recipientName", "Test",

-"recipientPhone", "13900139000",

-"recipientAddress", "Shanghai"

-));

-// 不设置 userId attribute

-mockMvc.perform(post("/api/orders")

-.contentType(MediaType.APPLICATION_JSON)

-.content(json))

-.andExpect(result -> {

-// 可能是 NPE 或 BusinessException，取决于实现

-assertTrue(result.getResolvedException() != null ||

-result.getResponse().getStatus() == 500 ||

-result.getResponse().getStatus() == 401,

-"未登录时应返回错误");

-});

-}

-

-// OCT-03: list_分页 — 返回当前用户的订单列表

-@Test

-void list_Paged() throws Exception {

+@Test @Order(1)

+void testList_ReturnsOk() throws Exception {

 mockMvc.perform(get("/api/orders")

-.header("Authorization", "Bearer " + userToken)

-.requestAttr("userId", 1L)

-.requestAttr("username", "testuser")

-.requestAttr("role", "USER"))

+.with(withUser(USER_ID)))

 .andExpect(status().isOk())

 .andExpect(jsonPath("$.code").value(200));

 }

 

-// OCT-04: detail_含明细 — 返回 order + items

-@Test

-void detail_WithItems() throws Exception {

+@Test @Order(2)

+void testDetail_ReturnsOk() throws Exception {

 mockMvc.perform(get("/api/orders/1")

-.header("Authorization", "Bearer " + userToken)

-.requestAttr("userId", 1L)

-.requestAttr("username", "testuser")

-.requestAttr("role", "USER"))

+.with(withUser(USER_ID)))

 .andExpect(status().isOk());

 }

 

-// OCT-05: pay_成功 — 200 OK

-@Test

-void pay_Success() throws Exception {

+@Test @Order(3)

+void testSubmit_NotLoggedIn() throws Exception {

+String oJson = "{\"recipientName\":\"Zhang\"," +

+"\"recipientPhone\":\"13800138000\"," +

+"\"recipientAddress\":\"Beijing\"}";

+mockMvc.perform(post("/api/orders")

+.contentType(MediaType.APPLICATION_JSON)

+.content(oJson))

+.andExpect(status().isOk())

+.andExpect(jsonPath("$.code").value(401));

+}

+

+@Test @Order(4)

+void testSubmit_Success() throws Exception {

+Long userId = 300L;

+

+// First create a product

+String pJson = "{\"name\":\"order_prod\",\"description\":\"test\",\"price\":99.99,\"stock\":50}";

+mockMvc.perform(post("/api/admin/products")

+.with(withAdmin())

+.contentType(MediaType.APPLICATION_JSON)

+.content(pJson));

+

+// Get the product ID from list

+String listResp = mockMvc.perform(get("/api/admin/products")

+.with(withAdmin()))

+.andReturn().getResponse().getContentAsString();

+var json = objectMapper.readTree(listResp);

+var records = json.get("data").get("records");

+long productId = -1;

+if (records != null && records.size() > 0) {

+productId = records.get(0).get("id").asLong();

+}

+

+// Add to cart

+String cJson = "{\"productId\":" + productId + ",\"quantity\":2}";

+mockMvc.perform(post("/api/cart")

+.with(withUser(userId))

+.contentType(MediaType.APPLICATION_JSON)

+.content(cJson));

+

+// Submit order

+String oJson = "{\"recipientName\":\"Zhang\"," +

+"\"recipientPhone\":\"13800138000\"," +

+"\"recipientAddress\":\"Beijing\"}";

+mockMvc.perform(post("/api/orders")

+.with(withUser(userId))

+.contentType(MediaType.APPLICATION_JSON)

+.content(oJson))

+.andExpect(status().isOk())

+.andExpect(jsonPath("$.code").value(200));

+}

+

+@Test @Order(5)

+void testPay_Success() throws Exception {

 mockMvc.perform(post("/api/orders/1/pay")

-.header("Authorization", "Bearer " + userToken)

-.requestAttr("userId", 1L)

-.requestAttr("username", "testuser")

-.requestAttr("role", "USER"))

+.with(withUser(USER_ID)))

 .andExpect(status().isOk());

 }

 

-// OCT-06: cancel_成功 — 200 OK，status=5

-@Test

-void cancel_Success() throws Exception {

+@Test @Order(6)

+void testCancel_Success() throws Exception {

 mockMvc.perform(post("/api/orders/1/cancel")

-.header("Authorization", "Bearer " + userToken)

-.requestAttr("userId", 1L)

-.requestAttr("username", "testuser")

-.requestAttr("role", "USER"))

+.with(withUser(USER_ID)))

 .andExpect(status().isOk());

 }

 

-// OCT-07: complete_成功 — 200 OK，status=4

-@Test

-void complete_Success() throws Exception {

+@Test @Order(7)

+void testComplete_Success() throws Exception {

 mockMvc.perform(post("/api/orders/1/complete")

-.header("Authorization", "Bearer " + userToken)

-.requestAttr("userId", 1L)

-.requestAttr("username", "testuser")

-.requestAttr("role", "USER"))

+.with(withUser(USER_ID)))

 .andExpect(status().isOk());

 }

 }

