--- current/shop-backend\pom.xml
+++ unmerged/shop-backend\pom.xml
@@ -22,9 +22,14 @@
 <java.version>17</java.version>

 <mybatis-plus.version>3.5.6</mybatis-plus.version>

 <jjwt.version>0.12.5</jjwt.version>

+<lombok.version>1.18.46</lombok.version>

+<byte-buddy.version>1.18.10</byte-buddy.version>

+<mockito.version>5.7.0</mockito.version>

+<argLine>-Djdk.attach.allowAttachSelf=true</argLine>

 </properties>

 

 <dependencies>

+<!-- Spring Boot Starters -->

 <dependency>

 <groupId>org.springframework.boot</groupId>

 <artifactId>spring-boot-starter-security</artifactId>

@@ -42,18 +47,21 @@
 <artifactId>spring-boot-starter-data-redis</artifactId>

 </dependency>

 

+<!-- MyBatis-Plus -->

 <dependency>

 <groupId>com.baomidou</groupId>

 <artifactId>mybatis-plus-spring-boot3-starter</artifactId>

 <version>${mybatis-plus.version}</version>

 </dependency>

 

+<!-- MySQL -->

 <dependency>

 <groupId>com.mysql</groupId>

 <artifactId>mysql-connector-j</artifactId>

 <scope>runtime</scope>

 </dependency>

 

+<!-- JWT -->

 <dependency>

 <groupId>io.jsonwebtoken</groupId>

 <artifactId>jjwt-api</artifactId>

@@ -72,17 +80,21 @@
 <scope>runtime</scope>

 </dependency>

 

+<!-- Lombok -->

 <dependency>

 <groupId>org.projectlombok</groupId>

 <artifactId>lombok</artifactId>

+<version>${lombok.version}</version>

 <optional>true</optional>

 </dependency>

 

+<!-- Jackson -->

 <dependency>

 <groupId>com.fasterxml.jackson.core</groupId>

 <artifactId>jackson-databind</artifactId>

 </dependency>

 

+<!-- Test 单元测试依赖 -->

 <dependency>

 <groupId>org.springframework.boot</groupId>

 <artifactId>spring-boot-starter-test</artifactId>

@@ -93,10 +105,36 @@
 <artifactId>h2</artifactId>

 <scope>test</scope>

 </dependency>

+<!-- JaCoCo 运行时（离线模式需要） -->

+<dependency>

+<groupId>org.jacoco</groupId>

+<artifactId>org.jacoco.agent</artifactId>

+<version>0.8.15</version>

+<classifier>runtime</classifier>

+<scope>test</scope>

+</dependency>

 </dependencies>

 

 <build>

 <plugins>

+<!-- Maven编译插件 适配Lombok注解 -->

+<plugin>

+<groupId>org.apache.maven.plugins</groupId>

+<artifactId>maven-compiler-plugin</artifactId>

+<configuration>

+<release>${java.version}</release>

+<encoding>UTF-8</encoding>

+<annotationProcessorPaths>

+<path>

+<groupId>org.projectlombok</groupId>

+<artifactId>lombok</artifactId>

+<version>${lombok.version}</version>

+</path>

+</annotationProcessorPaths>

+</configuration>

+</plugin>

+

+<!-- SpringBoot打包插件 -->

 <plugin>

 <groupId>org.springframework.boot</groupId>

 <artifactId>spring-boot-maven-plugin</artifactId>

@@ -110,31 +148,37 @@
 </configuration>

 </plugin>

 

+<!-- JaCoCo 离线插桩模式（避免 javaagent 与 JDK 26 不兼容） -->

 <plugin>

 <groupId>org.jacoco</groupId>

 <artifactId>jacoco-maven-plugin</artifactId>

-<version>0.8.12</version>

-<configuration>

-<destFile>${java.io.tmpdir}/shop-backend-jacoco.exec</destFile>

-<dataFile>${java.io.tmpdir}/shop-backend-jacoco.exec</dataFile>

-<append>true</append>

+<version>0.8.15</version>

+<configuration>

 <excludes>

-<exclude>com/shop/entity/*</exclude>

-<exclude>com/shop/dto/*</exclude>

-<exclude>com/shop/repository/*</exclude>

-<exclude>com/shop/config/*</exclude>

-<exclude>com/shop/ShopApplication.class</exclude>

+<exclude>com/shop/common/BusinessException.class</exclude>

 </excludes>

 </configuration>

 <executions>

 <execution>

-<goals>

-<goal>prepare-agent</goal>

+<id>instrument</id>

+<phase>prepare-package</phase>

+<goals>

+<goal>instrument</goal>

+</goals>

+</execution>

+<execution>

+<id>restore</id>

+<phase>verify</phase>

+<goals>

+<goal>restore-instrumented-classes</goal>

 </goals>

 </execution>

 <execution>

 <id>report</id>

 <phase>verify</phase>

+<configuration>

+<dataFile>${basedir}/jacoco.exec</dataFile>

+</configuration>

 <goals>

 <goal>report</goal>

 </goals>

@@ -142,15 +186,31 @@
 </executions>

 </plugin>

 

+<!-- Surefire 测试报告 -->

 <plugin>

 <groupId>org.apache.maven.plugins</groupId>

 <artifactId>maven-surefire-plugin</artifactId>

 <version>3.1.2</version>

 <configuration>

-<argLine>@{argLine} -Xmx512m</argLine>

-</configuration>

+<argLine>-Djdk.attach.allowAttachSelf=true -Djacoco.destFile=${project.build.directory}/jacoco.exec</argLine>

+</configuration>

+</plugin>

+

+<!-- Surefire 报告插件（生成HTML测试报告） -->

+<plugin>

+<groupId>org.apache.maven.plugins</groupId>

+<artifactId>maven-surefire-report-plugin</artifactId>

+<version>3.1.2</version>

+<executions>

+<execution>

+<id>generate-report</id>

+<phase>verify</phase>

+<goals>

+<goal>report</goal>

+</goals>

+</execution>

+</executions>

 </plugin>

 </plugins>

 </build>

-

 </project>

