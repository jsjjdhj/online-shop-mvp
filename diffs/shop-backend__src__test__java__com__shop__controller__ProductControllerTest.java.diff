--- current/shop-backend\src\test\java\com\shop\controller\ProductControllerTest.java
+++ unmerged/shop-backend\src\test\java\com\shop\controller\ProductControllerTest.java
@@ -1,52 +1,79 @@
 package com.shop.controller;

 

-import org.junit.jupiter.api.Test;

+import com.fasterxml.jackson.databind.ObjectMapper;

+import com.shop.ShopApplication;

+import com.shop.util.TestHelper;

+import org.junit.jupiter.api.*;

 import org.springframework.beans.factory.annotation.Autowired;

 import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;

 import org.springframework.boot.test.context.SpringBootTest;

+import org.springframework.http.MediaType;

 import org.springframework.test.web.servlet.MockMvc;

+import org.springframework.test.web.servlet.request.RequestPostProcessor;

+import org.springframework.transaction.annotation.Transactional;

 

-import static org.junit.jupiter.api.Assertions.*;

 import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;

 import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

 

-@SpringBootTest(classes = com.shop.ShopApplication.class)

+@SpringBootTest(classes = ShopApplication.class)

 @AutoConfigureMockMvc

+@Transactional

+@TestMethodOrder(MethodOrderer.OrderAnnotation.class)

 class ProductControllerTest {

 

 @Autowired private MockMvc mockMvc;

+@Autowired private ObjectMapper objectMapper;

 

-// PCT-01: list_分页搜索 — 返回 PageResult<Product>

-@Test

-void list_PagedSearch() throws Exception {

-mockMvc.perform(get("/api/products")

-.param("page", "1")

-.param("size", "10"))

+private static RequestPostProcessor withAdmin() {

+String token = TestHelper.createAdminToken(1L, "admin");

+return request -> {

+request.addHeader("Authorization", "Bearer " + token);

+return request;

+};

+}

+

+@Test @Order(1)

+void testList_Pagination() throws Exception {

+// Create a product first so the list is non-empty

+String pJson = "{\"name\":\"公开商品\",\"description\":\"test\",\"price\":29.99,\"stock\":100}";

+mockMvc.perform(post("/api/admin/products")

+.with(withAdmin())

+.contentType(MediaType.APPLICATION_JSON)

+.content(pJson));

+

+mockMvc.perform(get("/api/products"))

 .andExpect(status().isOk())

 .andExpect(jsonPath("$.code").value(200))

 .andExpect(jsonPath("$.data.records").isArray());

 }

 

-// PCT-02: detail_存在 — 返回商品对象

-@Test

-void detail_Exists() throws Exception {

-mockMvc.perform(get("/api/products/1"))

+@Test @Order(2)

+void testDetail_Exists() throws Exception {

+// Create a product first

+String pJson = "{\"name\":\"detail_prod\",\"description\":\"test\",\"price\":29.99,\"stock\":100}";

+mockMvc.perform(post("/api/admin/products")

+.with(withAdmin())

+.contentType(MediaType.APPLICATION_JSON)

+.content(pJson));

+

+// Get the first product ID from list

+String listResp = mockMvc.perform(get("/api/admin/products")

+.with(withAdmin()))

+.andReturn().getResponse().getContentAsString();

+var json = objectMapper.readTree(listResp);

+var records = json.get("data").get("records");

+if (records != null && records.size() > 0) {

+long id = records.get(0).get("id").asLong();

+mockMvc.perform(get("/api/products/" + id))

 .andExpect(status().isOk())

 .andExpect(jsonPath("$.code").value(200));

 }

+}

 

-// PCT-03: detail_不存在 — ID无效 → 返回错误响应

-@Test

-void detail_NotExists() throws Exception {

+@Test @Order(3)

+void testDetail_NotExists() throws Exception {

 mockMvc.perform(get("/api/products/99999"))

-.andExpect(result -> {

-String content = result.getResponse().getContentAsString();

-// 验证返回的是结构化 Result 响应（非原始异常）

-assertTrue(content.contains("\"code\""),

-"不存在的商品应返回结构化错误 JSON");

-// code 应该不是 200

-assertFalse(content.contains("\"code\":200"),

-"不存在的商品不应返回成功");

-});

+.andExpect(status().isOk())

+.andExpect(jsonPath("$.code").value(400));

 }

 }

